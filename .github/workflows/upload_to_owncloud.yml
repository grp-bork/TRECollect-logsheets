name: Upload to ownCloud

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'logsheets/**'
      - 'teams/**'
      - 'images/**'
  workflow_dispatch:

jobs:
  upload:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Fetch full history for git diff
      
      - name: Fetch base branch
        run: |
          BRANCH_NAME=${GITHUB_REF#refs/heads/}
          echo "Branch name: $BRANCH_NAME"
          git fetch origin $BRANCH_NAME
          git branch -a
          echo "HEAD commit: $(git rev-parse HEAD)"
          echo "origin/$BRANCH_NAME commit: $(git rev-parse origin/$BRANCH_NAME 2>/dev/null || echo 'not found')"
    
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
    
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
    
      - name: Upload to ownCloud
        env:
          OWNCLOUD_ACCESS_TOKEN: ${{ secrets.OWNCLOUD_ACCESS_TOKEN }}
          GITHUB_BEFORE: ${{ github.event.before }}
          GITHUB_AFTER: ${{ github.event.after }}
        run: |
          python upload_to_owncloud.py
